import { SchnipselConfig } from "./types";

/**
 * Sugar function to get intellisense while writing configuration for Schnipsel
 *
 * @param config - The configuration for Schnipsel
 *
 * @returns The same configuration opbject
 */
export const defineSchnipselConfig = (config: SchnipselConfig) => {
	return config;
};
